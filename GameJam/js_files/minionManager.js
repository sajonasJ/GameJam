class MinionManager{
    constructor(){
        this.sprite;
    }

    load(){//preload
    }

    setup(){// going to run at setup
        this.sprite=this.makeMinion
    }

    draw(){// going to run at draw

    }

    makeChar(x,y,size){ // createsprite
        let minion1=createSprite(x,y,size);
        //minion1.addImage();

        return char1
    }


}